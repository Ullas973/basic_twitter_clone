<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.7.0/fonts/remixicon.css"
    rel="stylesheet"
/>
</head>
<body>
    <div class="w-full min-h-screen h-auto bg-zinc-900 text-white p-5">

           <!-- <h1 class=" px-5 text-3xl font-bold block mb-10">Ye tho profile page hai bhai</h1> -->
            <a class="bg-blue-700 absolute right-30 top-5 text-white rounded-md px-4 py-2 font-bold " href="/profile/upload">Upload profile pic</a>
           <a class="bg-red-700 absolute right-5 top-5 text-white rounded-md px-4 py-2 font-bold " href="/logout">logout</a>
           <div class="flex gap-4 items-center justfy-center">
            <div class="h-12 w-12 object-cover rounded-full overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic %>" alt="">
            </div>
            <h1 class=" text-3xl font-bold block mb-3">Hey, <%= user.username %> 🙋‍♂️</h1>
           </div>
           
           <h2 class=" text-xl">You can create a post!</h1>
           
            <form action="/post" method="post">
                <textarea class="textarea block mt-10 p-5 outline-none lg:w-1/3 w-2/3 resize-none border-2 border-zinc-700 rounded-md " placeholder="What's on your mind!?" name="content"></textarea>
                <input type="submit" class="px-4 py-2 bg-blue-500 rounded-md block mt-5 text-white" value="Submit post">
            </form>

            <div class="posts mt-10">
                <h3 class="text-zinc-400">Your posts.</h3>
                <div class="postcontainer flex flex-col gap-1">
                   <% allPosts.reverse().forEach(function(post){ %>
                       <div class="post relative lg:w-1/3 w-2/3 p-2 mt-3 border-2 border-zinc-700 bg-zinc-800 outline-none rounded-md">
                        <a href="/profile/<%= post._id%>"><i class="ri-close-line absolute right-2 top-2"></i></a>
                    <h4 class="text-xl text-blue-500 mb-2">@<%= post.user.username%></h4>
                    <p class="text-s"><%= post.content%></p>
                    <small class="inline-block mt-5"><%= post.likes.length%> likes</small>
                    <br>
                    <a class="text-xs text-blue-400 mt-2" href="/like/<%= post._id%>">Like</a> 
                     
                    </div>   

                   <% }) %>
                   
                  
                    
                 </div>
            </div>
       
    </div>
    
</body>
</html>